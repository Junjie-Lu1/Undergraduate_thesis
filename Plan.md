# 基于性别-情绪双任务学习的情绪EEG识别


**关键词**：情绪识别，EEG，双任务学习，性别差异

## 数据集
[SEED系列 数据集](https://bcmi.sjtu.edu.cn/home/seed/index.html)；
[DEAP数据集](https://www.eecs.qmul.ac.uk/mmv/datasets/deap/);

这边数据集已经下载好了，**$\color{red}{TODO：查看哪些数据集是包含性别和情绪字段的，情绪都是几分类任务，查看筛选后的数据集的字段，进行大致的数据过滤，移除其他多余字段，方便得到干净样本。}$**(Done)

## 方法
采用双任务学习的框架，同时进行性别分类和情绪识别。具体步骤如下：
1. **数据预处理**：使用已经预处理好的特征，使用较长时间窗口（>=4秒），如果资源可以，也可以尝试自己进行数据预处理。(就使用已经处理好的吧，DE熵，Done)

   [这是EEG的处理库](https://github.com/braindecode/braindecode/tree/master)(不用管了，数据已经处理好)
2. **模型设计**：复现论文中的模型作为基座，再设计两个分类头，分别进行性别分类和情绪识别。(不复现了，好多自己搞一个简单的模型就行，模型已经设计好了)
   
    参考以下：
   - https://blog.csdn.net/mantoudamahou/article/details/134990549
   - https://zhuanlan.zhihu.com/p/667108069
   - https://github.com/eeyhsong/EEG-Conformer
   - https://github.com/braindecode/braindecode/tree/master
   - https://www.scholat.com/teamwork/showPostMessage.html?id=17215
   - $\color{red}{这块的模型挺多的，Github里面有很多仓库，还是要确认一下效果，有个 CNN+BiLSTM+Attention 的架构，好像有 0.99 的准确率，这块暂时还没看到开源的，但感觉用 Pytorch 实现起来应该不会很难。}$

   - $\color{red}{还有个EEG-Conformer，是处理 EEG 的潜在向量表示的，后面接个头就可以预测分类，这个有开源代码，可以配置一下看看}$(这个看了一下，是处理原始数据的，用处不大，但可以参照一下里面的数据增强方法)
   
4. **损失函数**设计联合损失函数，综合考虑性别分类和情绪识别的准确性，促进模型学习到更具代表性的特征，因为主要是情绪识别，所以可以设置情绪识别的损失权重稍微高一些。

   $\color{red}{这一块已经搞定了，在 Loss-Function-Design.md 里面，分别设置不同的损失函数进行对比即可，里面还有个自设优化器。}$

6. **评估与验证**：
   - 通过交叉验证或独立测试集评估模型的性能(可以考虑改动版本subject-dependent cross trial的实验，即随机匹配一位男性和女性的数据放一起训练)，比较联合预测的任务是否优于单一任务的预测效果（准确率、F1-score等指标外，也需要讨论对表示学习的影响）。

   - $\color{red}{这块不是很懂，老师还没回复，不过没什么所谓，这个是比较靠后的阶段了。}$
     
   - 关于表示学习观察，可以把模型中间层的特征提取，用降维算法可视化，观察聚类效果，可以体现模型表示学习的质量，还可以使用类内、类间的距离这种量化指标。除此之外，也可以冻结前面层，专门训练其他分类器，和之前的对比，如果效果更好，说明是表示学习效果增强的功劳。

   - 可以进行鲁棒性测试，对数据增加些许噪声，观察模型的鲁棒性。(工作量太大了，这块不做了)
   
   - 分析任务相关性，衡量不同性别的EEG的差异，差异大则说明任务有用的可能性大。

   - $\color{red}{这块也是比较靠后的阶段了。}$
     
7. **结果分析**：分析模型在性别和情绪两个任务上的表现，探讨性别差异对情绪识别的影响，并提出相应的改进建议。

   $\color{red}{这块也是比较靠后的阶段了。}$


# 行文结构

## 引言，这块就是抄嘛
### 研究背景与现状
就讲讲故事，脑机接口呀，抑郁症呀啥的。现状的话就是泛化性差。
### 研究价值与意义
就讲一下泛化性差，跨被试不太行。
### 国内外研究现状综述
随便讲讲这个领域的研究
### 研究内容与目标
讲一下研究的内容，要解决的问题
### 论文结构组织
介绍一下论文的组织格式
## 相关工作
就讲讲机器学习深度学习transformer那些咯
## 方法

### 架构设计
画个图
### 损失函数设计
写那五种
### 优化器设计
直接复制
## 实验验证

### 数据预处理
这里可以讲一些，可能可以有数据增强
### 模型训练
这里也交代得比较清楚一点，为什么这样选呀之类的（为什么全量微调？为什么不用CNN？这个在千问DeepRearch里面可能有，到时候看看，就是那些频段反正都有用，所以用注意力好点）
### 模型评估
评估有好多种方法：
- 同一个被试的其他trail训练，搞一点做测试集
- 老师说的那个改进版本的，就是原本是选一个人，现在又增选一个异性，那不就增加了性别了嘛
- 跨被试+交叉验证
- 最朴素那种，随便切分的
## 结果分析
这边做几个：
- 选最后一层，用t-SNE可视化，看看聚类明显不？类之间的距离如何？还有什么和聚类效果有关的指标？
- 看看任务是不是相关：
  - 算统计上的相关性，好像有个互信息还是啥，有个卡方检验，反正哪个有说服力贴哪个
  - 算梯度是否会相斥，算内积的符号嘛
  - 画每个男女在【特定的情绪下】的【某个频段】的时频曲线（62个算平均作为最终值）图，看看是不是有明显不同，假如太乱了的话就男生平均一下，女生平均一下再画，这样只有两条就看出来了。
  - 画每个男女在【特定的情绪下】的【某个频段】的时间与频率一起平均的柱状图，看看是不是有明显不同。
## 总结
看看qwen的那个记录里面还有没能补充的吧
## 附录
如果文章太长就放这
